models:
  - name: dim_customers
    description: marketing model for customers
    columns:
      - name: customer_id
        description: unique identifier for customer
        data_tests:
          - unique
          - not_null